- name: demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print names from loops
    ansible.builtin.debug:
      msg: " Hello, {{ item }}"
    loop:
    - ramesh
    - suresh
    - ramu
    - balu
    - govind
    - Devsecops